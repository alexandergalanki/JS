const myName="bobby";

function first(){
    const age=30;

    if(age>=30){
        const decade=3;
        var millenial=true;
    }
    function second(){
        const job="teacher";
        console.log(`${myName} is a ${age}year's-old ${job},${millenial}`);
    }
    second();
}
first();